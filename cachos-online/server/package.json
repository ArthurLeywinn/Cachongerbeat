{
  "name": "cachos-server",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor en tiempo real (Express + Socket.io) para Cachos / Liar's Dice",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
2. Borra server/package.json.json — ese archivo no debe existir.

Luego:

bash
git add server/package.json
git rm server/package.json.json
git commit -m "fix: correct server package.json with new dependencies"
git push


